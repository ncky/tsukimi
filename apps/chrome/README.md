# chrome

Tsukimi themes for Google Chrome.

## Install (unpacked)

1. Open `chrome://extensions`.
2. Enable Developer mode.
3. Click “Load unpacked”.
4. Select one of:
   - `apps/chrome/tsukimi-dark/`
   - `apps/chrome/tsukimi-light/`

