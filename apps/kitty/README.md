# kitty

This folder contains the Tsukimi kitty theme files.

## Install

1. Copy `tsukimi-*.conf` somewhere under your kitty config (commonly `~/.config/kitty/themes/`).
2. In `kitty.conf`, include one of them:
   - `include themes/tsukimi-dark.conf`
   - `include themes/tsukimi-light.conf`
